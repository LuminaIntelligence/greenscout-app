-r requirements.txt
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
pytest-cov>=5.0,<7.0
httpx>=0.28,<1.0
ruff>=0.7,<1.0
# Defekt CI-0 (2026-05-31): pyright exakt gepinnt um Version-Drift in CI zu verhindern.
# Aktuelle Versionen flaggen FastAPI/httpx TestClient generics als reportUnknownMemberType
# (siehe pyproject.toml [tool.pyright] executionEnvironments für tests/-Tuning).
pyright==1.1.391
