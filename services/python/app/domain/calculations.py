"""PV calculation logic.

Mirror of `src/lib/calculations/` on the TS side. The authoritative
implementation (and tests asserting parity with TS) is added by T-033 /
T-034. Constants used by these calculations live in `constants.py`.
"""

# TODO(T-033): implement calculation functions per SPEC §4.7.
