// TODO(T-002+): replace stub when real shared types land.
// Temporary export used by `src/app/page.tsx` to prove that the
// `@/types/*` path alias resolves end-to-end (tsc + Next runtime).

export type SetupPhase = "T-001" | "T-002" | "later";
