// TODO(T-002+): replace stub when real lib code lands.
// Temporary export used by `src/app/page.tsx` to prove that the
// `@/lib/*` path alias resolves end-to-end (tsc + Next runtime).

export const SETUP_PHASE: string = "T-001 scaffolding";
