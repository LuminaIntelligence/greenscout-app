// TODO(T-002+): replace stub when real components code lands.
// Temporary export used by `src/app/page.tsx` to prove that the
// `@/components/*` path alias resolves end-to-end (tsc + Next runtime).

export const COMPONENT_PLACEHOLDER: string = "components-tree-ready";
