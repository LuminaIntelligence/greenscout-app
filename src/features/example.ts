// TODO(T-002+): replace stub when real features code lands.
// Temporary export used by `src/app/page.tsx` to prove that the
// `@/features/*` path alias resolves end-to-end (tsc + Next runtime).

export const FEATURE_PLACEHOLDER: string = "feature-tree-ready";
